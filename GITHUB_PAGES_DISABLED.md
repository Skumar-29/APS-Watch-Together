# GitHub Pages is not used

APS Watch Together uses Render for its room server. The workflow files in `.github/workflows` are intentionally manual-only and do not deploy GitHub Pages.

If the repository still shows an old `pages-build-deployment` run, that historical run can be ignored. It does not affect Render or the Chrome extension.
