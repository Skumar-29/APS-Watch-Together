{
  "name": "aps-watch-together-server",
  "version": "1.5.1",
  "private": true,
  "description": "Zero-dependency room, signalling, playback synchronization, direct invite and screen-presenter service for APS Watch Together.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test ../test/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
